services:
  web:
    image: ${DOCKER_IMAGE}:${DOCKER_TAG}
    ports:
      - "${APP_PORT}:5000"
    volumes:
      - ./src:/app
    environment:
      - FLASK_ENV=development
      - PYTHONUNBUFFERED=1
